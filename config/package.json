{
  "name": "playwright",
  "version": "1.0.0",
  "description": "pruebas",
  "main": "playwright.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.14.1"
  }
}
